---
title: Drop the legacy flag
priority: p3
status: stale
labels: [cli, cleanup]
---

# Drop the legacy flag

The old flag keeps an alias that nobody uses. Remove the alias and say so in the release notes.
